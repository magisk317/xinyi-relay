<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="com.google.android.gms.permission.AD_ID" />
    <uses-permission
        android:name="android.permission.RECEIVE_SMS"
        tools:node="remove" />
    <uses-permission
        android:name="android.permission.RECEIVE_MMS"
        tools:node="remove" />
    <uses-permission
        android:name="android.permission.READ_SMS"
        tools:node="remove" />
    <uses-permission
        android:name="android.permission.READ_PHONE_STATE"
        tools:node="remove" />
    <uses-permission
        android:name="android.permission.READ_CALL_LOG"
        tools:node="remove" />
    <uses-permission
        android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS"
        tools:node="remove" />

    <application android:networkSecurityConfig="@xml/network_security_config">
        <receiver
            android:name="io.github.magisk317.relay.receiver.StandardSmsReceiver"
            tools:node="remove" />
        <receiver
            android:name="io.github.magisk317.relay.receiver.StandardMmsReceiver"
            tools:node="remove" />
        <service
            android:name="io.github.magisk317.relay.service.AutoInputAccessibilityService"
            tools:node="remove" />
    </application>

</manifest>
