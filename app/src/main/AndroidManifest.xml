<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS" tools:ignore="ProtectedPermissions" />

    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />
        </intent>
    </queries>

    <application
        android:name="io.github.magisk317.relay.ui.app.SmsCodeApplication"
        android:allowBackup="true"
        android:description="@string/xposed_description"
        android:fullBackupContent="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:localeConfig="@xml/locales_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppNoActionBarTheme"
        tools:ignore="GoogleAppIndexingWarning,UnusedAttribute">

        <receiver
            android:name="io.github.magisk317.relay.receiver.SecretCodeReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.provider.Telephony.SECRET_CODE" />
                <data
                    android:host="7672633"
                    android:scheme="android_secret_code" />
            </intent-filter>
        </receiver>



        <activity
            android:name="io.github.magisk317.relay.ui.home.LauncherActivity"
            android:enabled="true"
            android:exported="true"
            android:theme="@android:style/Theme.NoDisplay">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="io.github.magisk317.relay.ui.home.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.INFO" />
                <category android:name="de.robv.android.xposed.category.MODULE_SETTINGS" />
            </intent-filter>
            <!-- Intent filters for rule import -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:host="*" android:mimeType="*/*" android:pathPattern="/.*\\.scebak" android:scheme="file" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:host="*" android:mimeType="*/*" android:pathPattern="/.*\\.scebak" android:scheme="content" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:mimeType="application/octet-stream" android:scheme="content" />
                <data android:mimeType="application/json" android:scheme="content" />
            </intent-filter>
        </activity>


        <provider
            android:name="io.github.magisk317.relay.data.db.DBProvider"
            android:authorities="${applicationId}.db.provider"
            android:exported="true" />

        <provider
            android:name="io.github.magisk317.relay.data.prefs.PrefsProvider"
            android:authorities="${applicationId}.pref.provider"
            android:exported="true" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.files"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/files" />
        </provider>

        <receiver
            android:name="io.github.magisk317.relay.xp.hook.code.AutoCancelReceiver"
            android:exported="false" />

        <!-- IPC: receives forwarded SMS payload from the Xposed hook layer.
             Must be exported=true because Xposed hooks run under the hooked app's UID (cross-UID).
             Security: intent.setPackage(packageName) in ForwardAction ensures only this module
             receives the broadcast; no external app can deliver to this receiver via explicit package targeting. -->
        <receiver
            android:name="io.github.magisk317.relay.receiver.ForwardReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="io.github.magisk317.relay.ACTION_FORWARD_SMS" />
            </intent-filter>
        </receiver>

        <service
            android:name="io.github.magisk317.relay.service.ForceStopRecoveryService"
            android:exported="true" />

        <service android:name="io.github.magisk317.relay.service.AppNotificationListenerService"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>
    </application>

</manifest>
