<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />

    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS" tools:ignore="ProtectedPermissions" />

    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />
        </intent>
    </queries>

    <application
        android:name="io.github.magisk317.relay.ui.app.SmsCodeApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:description="@string/xposed_description"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:localeConfig="@xml/locales_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppNoActionBarTheme"
        tools:ignore="GoogleAppIndexingWarning,UnusedAttribute">

        <!-- xposed module configuration -->
        <meta-data
            android:name="xposedmodule"
            android:value="true" />
        <meta-data
            android:name="xposeddescription"
            android:value="@string/xposed_description" />
        <meta-data
            android:name="xposedminversion"
            android:value="30" />
        <meta-data
            android:name="xposedscope"
            android:resource="@array/xposed_scope" />
        <meta-data
            android:name="xposedsharedprefs"
            android:value="true" />

        <receiver
            android:name="io.github.magisk317.relay.receiver.SecretCodeReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.provider.Telephony.SECRET_CODE" />
                <data
                    android:host="7672633"
                    android:scheme="android_secret_code" />
            </intent-filter>
        </receiver>



        <activity
            android:name="io.github.magisk317.relay.ui.home.LauncherActivity"
            android:enabled="true"
            android:exported="true"
            android:theme="@android:style/Theme.NoDisplay">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="io.github.magisk317.relay.ui.home.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.INFO" />
                <category android:name="de.robv.android.xposed.category.MODULE_SETTINGS" />
            </intent-filter>
            <!-- Intent filters for rule import -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:host="*" android:mimeType="*/*" android:pathPattern="/.*\\.scebak" android:scheme="file" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:host="*" android:mimeType="*/*" android:pathPattern="/.*\\.scebak" android:scheme="content" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:mimeType="application/octet-stream" android:scheme="content" />
                <data android:mimeType="application/json" android:scheme="content" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.files"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/files" />
        </provider>

        <service
            android:name="io.github.magisk317.relay.service.AutoInputAccessibilityService"
            android:exported="false"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>

            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/auto_input_accessibility_service" />
        </service>

        <receiver
            android:name="io.github.magisk317.relay.xp.hook.code.AutoCancelReceiver"
            android:exported="false" />

        <receiver
            android:name="io.github.magisk317.relay.xp.hook.code.CopyCodeReceiver"
            android:exported="false" />

        <!-- IPC: receives code-notification requests from the SMS hook process and posts the
             actual status-bar notification from the module app process, so the notification is
             owned by this app instead of com.android.phone. Token verification happens in the
             receiver to prevent spoofed external broadcasts. -->
        <receiver
            android:name="io.github.magisk317.relay.receiver.CodeNotificationReceiver"
            android:exported="true" />

        <receiver
            android:name="io.github.magisk317.relay.receiver.AutoInputResultReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="io.github.magisk317.relay.ACTION_AUTO_INPUT_RESULT" />
            </intent-filter>
        </receiver>

        <receiver
            android:name="io.github.magisk317.relay.receiver.LowBatteryReminderReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="io.github.magisk317.relay.ACTION_LOW_BATTERY_REMINDER" />
            </intent-filter>
        </receiver>

        <receiver
            android:name="io.github.magisk317.relay.receiver.CallAlertMonitorReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="io.github.magisk317.relay.ACTION_CALL_ALERT_MONITOR_REFRESH" />
            </intent-filter>
        </receiver>

        <service
            android:name="io.github.magisk317.relay.service.ForceStopRecoveryService"
            android:exported="true" />

        <service android:name="io.github.magisk317.relay.service.AppNotificationListenerService"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>
    </application>

</manifest>
