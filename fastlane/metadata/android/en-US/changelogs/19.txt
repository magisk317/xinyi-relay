- Updated to 0.1.0 (19).
- Android Agent now better supports the Docker backend flow with remote binding, config snapshots, record upload, and local HTTPS access via Caddy.
- Verification codes blocked on ROM-specific SMS dispatch paths can now still be forwarded, not just autofilled.
- Fallback code records are imported back into the app automatically after startup.
- Details: https://github.com/magisk317/xinyi-relay/releases
